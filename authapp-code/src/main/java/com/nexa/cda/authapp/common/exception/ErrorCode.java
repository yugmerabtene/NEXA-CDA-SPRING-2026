package com.nexa.cda.authapp.common.exception;

public enum ErrorCode {
    BUSINESS_ERROR,
    VALIDATION_ERROR,
    EMAIL_ALREADY_USED,
    INVALID_CREDENTIALS,
    USER_NOT_FOUND,
    UNAUTHORIZED,
    FORBIDDEN,
    INTERNAL_ERROR
}
